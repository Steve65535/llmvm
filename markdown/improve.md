需要改进的地方
1. 这个系统目前缺乏操作真实文件的能力 需要增加一个文件操作的接口 就是一个cli就行 然后解析器可以解析命令 操作cli
2. 目前的上下文窗口只能感知当前枝的临时变量 但是对于宏大项目 这个感知信息明显是不足的 我希望你在创建一个tree时候 通过给每一个tasknode编号 然后采用滑动窗口 查看历史的那些节点的内部信息包含现在这个节点的所需要的信息 然后pick出来 摘取对当前节点有用的信息放入当前节点上下文 实现全局注意力感知
3. 对于leaf节点 你的理解稍微有点偏差 不是元任务 二是任务的上下文窗口可以完美的fit进大模型的最佳上下文窗口 如果一个很大的元任务 仍然可能撑破上下文窗口
